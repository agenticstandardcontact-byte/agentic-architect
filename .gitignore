# --------------------------------------------------------------------
# LOCAL-ONLY DEV NOTES, DRAFTS, AND MARKETING WORKING FILES
# Anything matching these patterns stays on disk and never gets pushed.
scripts/__pycache__/
node_modules/
.env
.env.*
!.env.example

# Apollo / cold-outreach exports (PII — never commit)
apollo-contacts-export.csv
apollo-*.csv
apollo-outbox/

# Operator / agent session notes — local only (never public)
LEARNING_LOG.md
TOKEN_EFFICIENCY_RULES.md
SESSION_HISTORY.md
HANDOFF*.md
MANUAL_TASKS.md
MASTER_PLAN*.md
**/LEARNING_LOG.md
**/TOKEN_EFFICIENCY_RULES.md

# Local tooling — not part of the published site
scripts/
.cursor/
.env.example
.mailmap
package.json
package-lock.json
playwright*.config.*
*.mjs
!comparison-demo.js
