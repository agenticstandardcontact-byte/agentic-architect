# Product screenshots (user-provided)

Add these files so the homepage proof section renders images:

- `cursor-rules-folder.png` — `.cursor/rules/` in a real .NET solution
- `learning-log-md.png` — `LEARNING_LOG.md` with ADR-style entries
- `hydrate-workflow.gif` — hydrate prompt seeding the log (optional GIF)

Recommended size: 1120×630 or 16:9 PNG/WebP. Alt text is already set in `index.html`.
