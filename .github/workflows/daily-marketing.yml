name: Daily Marketing Engine
on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Update Tip
        run: |
          # 1. Grab a random rule that starts with 'Rule' from your library
          SELECTED_RULE=$(grep "Rule " rules-library.md | shuf -n 1)
          
          # 2. Add a clear header and the rule to the end of the README
          echo -e "\n\n---\n### 🛠️ Daily Senior Rule Update\n> $SELECTED_RULE\n" >> README.md
          
      - name: Commit and Push
        run: |
          git config --global user.name "Agentic Architect Bot"
          git config --global user.email "bot@agenticarchitect.com"
          git add README.md
          git commit -m "Auto-update: Daily Tip" || exit 0
          git push
